const font = {};
const colors = {
  primary: "#841584",
  white: "#FFFFFF",
};
const images = {};
const strings = {};
const icons = {
  rightArrow: require("../img/rightIcon.png"),
  closeIcon: require("../img/closeIcon.png"),
};

export default { font, icons, strings, colors, images };
